#include <stdio.h>
#include <string.h>

int max(int a, int b) {
    
    if (a > b) 
        return a;
     else 
        return b;
}
    
    
int main() 
{
    int max_num = max(10, 20);
    printf("Максимальное число: %d\n", max_num);
    
    return 0;
}
